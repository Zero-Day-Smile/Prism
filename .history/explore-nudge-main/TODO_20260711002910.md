## Lovable removal (standalone auth)

### Step 0 — Repo understanding
- [x] Located current Google sign-in flow in `src/routes/auth.tsx` (uses `prismOAuth`).
- [x] Inspected `src/integrations/prism-oauth/index.ts` (wrapper over Supabase OAuth).
- [x] Inspected Supabase client config in `src/integrations/supabase/client.ts`.

### Step 1 — Update Auth route
- [x] Replace `signInGoogle()` implementation to call `supabase.auth.signInWithOAuth({ provider: 'google' ... })` directly.
- [x] Remove `prismOAuth` import.
- [ ] Update `useEffect` to clean URL hash/access_token and navigate with `replace`.


### Step 2 — Remove Lovable/prism OAuth wrappers
- [ ] Remove Lovable-specific imports/references found during search.
- [ ] Uninstall Lovable package(s) from `package.json`/lockfile.

### Step 3 — Supabase PKCE
- [ ] Ensure `createClient` uses `auth.flowType = 'pkce'` (either by editing generated file or moving to a non-generated client).

### Step 4 — Verify
- [ ] Run typecheck/build.
- [ ] Smoke test: email/password sign-in, Google OAuth redirect, and authenticated route redirect.

